# E2E test credentials — copy to .env.test and fill in values (never commit .env.test)
E2E_ADMIN_EMAIL=
E2E_ADMIN_PASSWORD=

# Optional: override dev server URL (defaults to http://127.0.0.1:5173)
# E2E_BASE_URL=http://127.0.0.1:5173

# Supabase (required for the app — copy from your local .env)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
